---
/**
 * @deprecated
 * Legacy helper from the starter theme.
 * Prefer navigation config + modern nav components in `src/data/navigation.ts` and `src/components/organisms/Navbar.astro`.
 */
import type { HTMLAttributes } from 'astro/types';

type Props = HTMLAttributes<'a'>;

const { href = '#', class: className, ...props } = Astro.props;
---

<a href={href} class={className} {...props}>
  <slot />
</a>
