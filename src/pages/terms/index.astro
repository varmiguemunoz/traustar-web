---
import LegalPageTemplate from '@/components/templates/LegalPageTemplate.astro';
import { termsPage } from '@/data/legal/terms';
---

<LegalPageTemplate
  seoTitle={termsPage.seoTitle}
  seoDescription={termsPage.seoDescription}
  pageTitle={termsPage.pageTitle}
  updatedAt={termsPage.updatedAt}
  toc={termsPage.toc}
  sections={termsPage.sections}
  contactLabel={termsPage.contactLabel}
  contactEmail={termsPage.contactEmail}
/>
